import { defineStore } from 'pinia'

export const useSystemStore = defineStore('system', {
  state: () => ({
    // 系统状态，管理系统配置、通知设置等
  }),
  getters: {
    // getters
  },
  actions: {
    // actions
  }
})