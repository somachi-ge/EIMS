import { defineStore } from 'pinia'

export const usePlatformStore = defineStore('platform', {
  state: () => ({
    // 平台状态，管理平台信息、平台状态等
  }),
  getters: {
    // getters
  },
  actions: {
    // actions
  }
})