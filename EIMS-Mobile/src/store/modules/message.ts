import { defineStore } from 'pinia'

export const useMessageStore = defineStore('message', {
  state: () => ({
    // 消息状态，管理消息列表、未读消息数等
  }),
  getters: {
    // getters
  },
  actions: {
    // actions
  }
})