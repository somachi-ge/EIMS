import { defineStore } from 'pinia'

export const useApplicationStore = defineStore('application', {
  state: () => ({
    // 应用状态，管理应用列表、应用状态等
  }),
  getters: {
    // getters
  },
  actions: {
    // actions
  }
})